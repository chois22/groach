// 상태를 나타날 때 status
enum StatusOfPw { none, match, not_match }

enum ProgramType { recommend, popular, hot, brand_new, hokangs, farm }

enum ServiceType { wifi, pet, parking }

enum ReviewKeyword {
  good_facility, // 시설이 좋아요
  kind_owner, // 사장님이 착해요
}
